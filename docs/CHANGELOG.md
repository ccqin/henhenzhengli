# 版本更新日志

> 流程约定（2026-09-04 起）：日常改动只提交代码并记录到「待发布」清单，
> **不打包、不部署、不递增版本号**；由用户明确说"更新版本"时统一打包部署 + 商店提交。

## 已发布版本

### 1.0.9.0（2026-09-04，本地部署；商店提交待用户发起）

- fix: 启动时"退出期间新增的文件"叠在左上角——放置分两遍（先存量后新文件，新文件正确找空位）
- feat: 无关联文件双击弹系统「打开方式」对话框（SHOpenWithDialog，可顺带设默认）
- feat: 桌面操作三件套
  - 拖拽保持抓取偏移（按下点=抬起点，原生手感）
  - 放置碰撞检测（拖到图标上松手自动让位到最近空格，杜绝叠加）
  - 桌面键入快速查找（打字母选中匹配图标；仅前台为桌面时响应，其他应用打字零影响）
- fix: "对齐图标"从排序重排改为就近吸附（保持排列，歪的归位）+ 排序后左上空格修复
- perf: 视频循环回绕主动对齐（短视频跨屏循环无跳变）；VLC 噪声日志过滤
- fix: 跨屏移动"此电脑"桌面假死黑屏（图标层日志污染 IPC 通道，双层修复）
- fix: MSIX 适配（StartupTask UI 线程死锁；启动项显示名中文化）
- feat: 设置窗口「关于与开源」页（ffmpeg GPL / VLC LGPL 声明 + 隐私说明）
- feat: config 自动备份 + 损坏回退；关键操作立即落盘；空快照防覆盖
- 部署链：deploy-local.ps1 一条龙（打包→签名→杀→卸→装→启动→验证）

### 1.0.2.0（2026-08-31，本地部署）

- fix: 切换壁纸 VLC 弃嵌入自建弹窗 + 位置上报重复
- fix: 壁纸子进程重启后图标层被盖（BottomPair 补齐）
- perf: 视频壁纸 HEVC 后台转码（VideoDecode 引擎 34%→19%）

### 1.0.0.0（2026-08-25）

- 首个商店提交版本：LibVLC DirectX 直渲染替换 MediaElement（GPU 第三梯队）

---

## 待发布（下次"更新版本"时打包）

（暂无——新改动记录在此，格式：日期 + 类型 + 一句话描述）
